# 세션 요약 — 채팅 이력을 요약 기반으로 업그레이드

- 세션: `d0eb96ca-1497-4fc1-a135-feba0032c772`
- 기간: 2026-08-04T01:12:44.218Z ~ 2026-08-05T08:26:13.910Z
- 브랜치: `feature/chat-history-limit`
- 도구 사용: Read 14회 · Bash 29회 · Edit 22회 · Write 1회 · Skill 2회 · Agent 1회
- 수정 파일 (12):
  - `/Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream/lib/types.ts`
  - `/Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream/app/api/chat/route.ts`
  - `/Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream/lib/chat-stream.ts`
  - `/Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream/lib/chat-history.ts`
  - `/Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream/lib/useChatSession.ts`
  - `/Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream/lib/chat-history.test.ts`
  - `/Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream/lib/chat-stream.test.ts`
  - `/Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream/app/api/chat/__tests__/route.test.ts`
  - `/Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream/lib/chat-stream.edge.test.ts`
  - `/Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream/docs/superpowers/specs/dream-app.html`
  - `/Users/lf/.claude/projects/-Users-lf-Desktop-gugbab-workspace-03-gugbab-claude-dream/memory/project_dream_history_summary.md`
  - `/Users/lf/.claude/projects/-Users-lf-Desktop-gugbab-workspace-03-gugbab-claude-dream/memory/MEMORY.md`

## 대화

### [1] 요청

채팅 이력 압축을 "요약 기반"으로 업그레이드하고 싶어. relay의 wantSummary
계약을 실제로 사용하는 작업이야.

## 배경 (이미 머지된 상태)
- lib/chat-history.ts: 전송 전 최근 30개 절단 + 메시지당 4000자 클램프 적용 중
- app/api/chat/route.ts: relay에 wantSummary: false 전달 중
- @gugbab/relay-types 1.0.0-202608031427 핀 완료
- relay 계약: wantSummary: true면 스트리밍 종료 시 done 이벤트에
  summary?: string(한국어 1~3문장, 300자 이내, best-effort — 실패 시 누락)이 실림

## 목표
긴 해몽 답변이 오래된 턴이 되면 원문 대신 요약을 전송해서, 절단으로 인한
맥락 손실을 줄인다. (같은 구조가 health 앱에 이미 프로덕션 적용돼 있음)

## 작업 내용
1. @gugbab/utils를 1.2.0 이상으로 업데이트 — SseEvent의 done 변형에
   summary?: string이 추가된 버전 (toSSELine 타입 그대로 사용 가능)

2. app/api/chat/route.ts
   - relay 요청 wantSummary: false → true
   - ⚠️ 중요: injectDoneFields()가 relay done 이벤트를
     { type: "done", sessionId, modelId }로 "교체"하는 구조라서
     지금 그대로면 relay가 보낸 summary가 유실됨.
     relay done의 summary를 보존해서 함께 실어야 함:
     { type: "done", sessionId, modelId, summary? }
   - lib/types.ts의 ChatSseEvent done 변형에도 summary?: string 추가

3. lib/chat-stream.ts
   - done 이벤트에서 summary를 읽어 호출자에게 전달
     (현재 modelId만 반환 — 반환값을 { modelId, summary? } 형태로 확장)

4. lib/useChatSession.ts + lib/types.ts
   - ChatMessage(role: "model")에 summary?: string 추가
     (IDB에 그대로 저장 — 선택 필드라 마이그레이션 불필요)
   - 응답 확정 시 aiMsg에 summary 부착

5. lib/chat-history.ts — toOutgoingMessages 확장
   - 최근 4개 메시지는 원문 유지 (클램프만)
   - 그보다 오래된 model 메시지는 summary가 있으면
     "[이전 답변 요약] " + summary로 대체, 없으면 기존처럼 절삭
   - 전송 직전 summary 필드는 제거하고 { role, content }만 전송
   - 요약이 best-effort라 없을 수 있음 — 없어도 동작이 깨지면 안 됨

6. 테스트
   - chat
…(생략)

**응답**

요청을 이렇게 이해했습니다: relay의 `wantSummary` 계약을 활성화해서, 오래된 model 메시지를 원문 대신 relay가 만들어준 요약으로 전송하도록 채팅 이력 압축을 업그레이드 — 사용자가 6단계 작업 목록과 검증 기준(테스트·typecheck·biome·빌드)까지 명시해 주셨으므로 이 목록을 승인된 계획으로 삼아 바로 진행하겠습니다. (DreamSession.summary 교체는 범위 밖 — 손대지 않습니다.)

먼저 관련 파일들을 읽어 현재 구조를 파악합니다.

이제 기존 테스트 파일들과 vitest 설정을 확인합니다.

1.2.0에 `summary?: string`이 확인됐습니다. 이제 코드를 수정합니다 — 먼저 타입부터.

이제 useChatSession.ts만 남았습니다:

타입 에러 해소 (훅 통과). 이제 테스트를 갱신합니다 — chat-history부터:

chat-stream.test.ts — 반환값 형태 변경 반영 + summary 파싱 테스트 추가:

대시보드에 이번 작업 단위의 phase 블록을 추가합니다:

완료했습니다. 요약 기반 이력 압축이 전 구간(relay → route → stream → IDB 저장 → 재전송 압축)으로 연결됐고, 모든 검증을 통과했습니다.

## 변경 내용

1. **`@gugbab/utils` 1.1.0 → 1.2.0** — `SseEvent`의 done 변형에 `summary?: string`이 추가된 버전으로 업데이트, `toSSELine` 그대로 사용.
2. **`app/api/chat/route.ts`** — `wantSummary: true` 전환. `injectDoneFields`가 relay done을 교체할 때 relay가 실어준 `summary`(문자열인 경우만 — 비문자열 계약 위반 값은 무시)를 보존해 `{ type: "done", sessionId, modelId, summary? }`로 주입.
3. **`lib/chat-stream.ts`** — `streamChat` 반환을 `string` → `StreamChatResult`(`{ modelId, summary? }`)로 확장. summary가 없으면 키 자체를 생략.
4. **`lib/useChatSession.ts` + `lib/types.ts`** — `ChatMessage`·`ChatSseEvent` done에 `summary?` 추가, 응답 확정 시 `aiMsg`에 summary 부착(IDB에 그대로 저장), 전송 시 summary를 `toOutgoingMessages`로 전달.
5. **`lib/chat-history.ts`** — 입력 타입 `HistoryMessage`(= OutgoingMessage + `summary?`) 신설. 최근 4개는 원문 유지(클램프만), 그보다 오래된 model 메시지는 summary가 있으면 `"[이전 답변 요약] " + summary`로 대체(대체 후에도 4000자 클램프 적용 — 과대 요약 방어), 없거나 빈 문자열이면 기존 절삭 폴백. 전송은 `{ role, content }`만.

## 검증 결과

- **vitest 98건 전부 통과** — 신규 테스트: chat-history 6건(요약 대체/폴백/빈 요약/최근 원문 유지/summary 필드 미전송/과대 요약 클램프), route 3건(summary 보존/누락 시 생략/비문자열 방어), chat-stream 2건 + `wantSummary: true` 기대값 갱신. 구 반환 계약(string)을 쓰던 `chat-stream.edge.test.ts` 4건도 함께 갱신했습니다.
- **adversarial 테스트의 relay body 키 목록은 갱신이 불필요했습니다** — `wantSummary` 키는 이전부터 존재했고 값만 `false → true`로 바뀌어 `["app", "messages", "systemPrompt", "wantSummary"]` 그대로 통과합니다.
- **typecheck·biome·프로덕션 빌드 모두 통과.**
- **Codex 적대적 리뷰 (1라운드 조기 종료)**: "no discrete regression or contract break" — 이슈 0건이라 Round 2·3 생략, 마커 기록 완료. Codex 샌드박스 내부에서 vitest가 EPERM으로 못 돌았지만 이는 Codex 실행 환경 권한 문제이고 로컬 실행은 전부 통과했습니다.
- 메모리 룰에 따라 `docs/superpowers/specs/dream-app.html`에 "이력 압축 v2" phase 블록을 추가했습니다.

## 커밋 분리 제안 (요청 시 실행)

1. `[app] Modify: 요약 기반 이력 압축 — relay wantSummary 활성화` — 코드 + 테스트 + package.json/lockfile
2. `[docs] Update: 대시보드에 이력 압축 v2 단계 추가` — dream-app.html

커밋은 요청하실 때 진행하겠습니다. 참고로 말씀 주신 판단 포인트대로 `DreamSession.summary`(첫 메시지 앞 50자) 교체는 범위 밖이라 손대지 않았습니다.

### [2] 요청

작업다 완료한건가??? 커밋 푸쉬하면돼?

**응답**

네, 작업은 전부 완료·검증됐습니다:

- **vitest 98건 통과** (요약 대체/폴백/summary 보존 등 신규 11건 포함)
- **typecheck · biome · 프로덕션 빌드 통과**
- **Codex 적대적 리뷰 이슈 0건** (조기 종료, 마커 기록됨)
- 대시보드(dream-app.html)도 갱신 완료

미커밋 변경은 12개 파일(코드 8 + 테스트 4... 정확히는 코드·테스트 10 + package.json/lockfile + 대시보드)입니다.

바로 커밋·푸시 가능한 상태인데, 한 가지만 — 이 레포 규칙상 **커밋·푸시 전에 메모리 정리 배치**(낡은 memory 갱신 → `session-export.js --refresh` → 미러 일치 확인)를 먼저 돌리고 `[memory]`/`[export]` 커밋을 같은 배치에 포함해야 deliverable-guard 훅이 push를 통과시킵니다.

"커밋해" 하시면 이 순서로 진행합니다:

1. 메모리 정리 + exports refresh + 미러 diff 확인
2. `[app] Modify: 요약 기반 이력 압축 — relay wantSummary 활성화` (코드·테스트·package.json·lockfile)
3. `[docs] Update: 대시보드에 이력 압축 v2 단계 추가`
4. `[memory]` / `[export]` 커밋 → 현재 브랜치(feature/chat-history-limit)로 push

PR 생성은 규칙대로 사용자께서 직접 하시면 됩니다. 진행할까요?

### [3] 요청

커밋 푸쉬 진행해줘

**응답**

네, 진행합니다. 규칙 순서대로 — 먼저 메모리 정리부터.

**1단계: 메모리 스캔·기록** — 기존 메모리 중 이번 작업과 어긋나는 서술은 없습니다. 프롬프트 범위 밖으로 보류된 후속 작업(DreamSession.summary 교체)은 코드에 남지 않는 결정이라 project 메모리로 기록합니다.

**2단계: 세션 요약 refresh + 미러 일치 확인**

exit 0인데 새 파일이 없습니다. 스크립트가 어떤 조건에서 파일을 쓰는지 확인합니다.

exports 생성 완료. 이제 관심사별로 커밋을 분리합니다.

커밋·푸시 완료했습니다. `feature/chat-history-limit` 브랜치에 4개 커밋이 올라갔고 워킹트리는 클린합니다:

| 커밋 | 내용 |
|------|------|
| `7f7ac45` `[app]` | 요약 기반 이력 압축 — 코드·테스트 11개 파일 (utils 1.2.0 포함) |
| `155ff2f` `[docs]` | 대시보드 이력 압축 v2 단계 추가 |
| `90730c3` `[memory]` | 후속 후보(DreamSession.summary AI 요약 교체) 기록 + 인덱스 |
| `5bace46` `[export]` | 세션 요약 동기화 |

진행 중 두 가지 이슈가 있었는데 모두 해결됐습니다: `session-export.js --refresh`가 `CLAUDE_PROJECT_DIR` 미설정으로 조용히 no-op 됐던 것은 환경변수를 명시해 재실행했고, docs 커밋에서 Cursor git lock 경합이 한 번 발생해 재시도 루프로 통과시켰습니다.

이제 규칙대로 PR 생성만 남았고, 이건 직접 해주시면 됩니다. 참고로 이 브랜치에는 이미 머지 안 된 이전 커밋(`6c00959` 이력 절단)이 함께 있으니 PR에는 두 작업(절단 + 요약 압축)이 같이 묶입니다.

### [4] 요청

먼저 리뷰 부터 진행해줘 완료하면 pr 이후 머지할게

**응답**

리뷰 요청이니 superpowers의 requesting-code-review 스킬을 먼저 로드해 절차대로 진행하겠습니다.

### [5] 요청

Base directory for this skill: /Users/lf/.claude/plugins/cache/superpowers-marketplace/superpowers/5.0.7/skills/requesting-code-review

# Requesting Code Review

Dispatch superpowers:code-reviewer subagent to catch issues before they cascade. The reviewer gets precisely crafted context for evaluation — never your session's history. This keeps the reviewer focused on the work product, not your thought process, and preserves your own context for continued work.

**Core principle:** Review early, review often.

## When to Request Review

**Mandatory:**
- After each task in subagent-driven development
- After completing major feature
- Before merge to main

**Optional but valuable:**
- When stuck (fresh perspective)
- Before refactoring (baseline check)
- After fixing complex bug

## How to Request

**1. Get git SHAs:**
```bash
BASE_SHA=$(git rev-parse HEAD~1)  # or origin/main
HEAD_SHA=$(git rev-parse HEAD)
```

**2. Dispatch code-reviewer subagent:**

Use Task tool with superpowers:code-reviewer type, fill template at `code-reviewer.md`

**Placeholders:**
- `{WHAT_WAS_IMPLEMENTED}` - What you just built
- `{PLAN_OR_REQUIREMENTS}` - What it should do
- `{BASE_SHA}` - Starting commit
- `{HEAD_SHA}` - Ending commit
- `{DESCRIPTION}` - Brief summary

**3. Act on feedback:**
- Fix Critical issues immediately
- Fix Important issues before proceeding
- Note Minor issues for later
- Push back if reviewer is wrong (with reasoning)

## Example

```
[Just completed Task 2: Add verificatio
…(생략)

### [6] 요청

Base directory for this skill: /Users/lf/.claude/plugins/cache/superpowers-marketplace/superpowers/5.0.7/skills/receiving-code-review

# Code Review Reception

## Overview

Code review requires technical evaluation, not emotional performance.

**Core principle:** Verify before implementing. Ask before assuming. Technical correctness over social comfort.

## The Response Pattern

```
WHEN receiving code review feedback:

1. READ: Complete feedback without reacting
2. UNDERSTAND: Restate requirement in own words (or ask)
3. VERIFY: Check against codebase reality
4. EVALUATE: Technically sound for THIS codebase?
5. RESPOND: Technical acknowledgment or reasoned pushback
6. IMPLEMENT: One item at a time, test each
```

## Forbidden Responses

**NEVER:**
- "You're absolutely right!" (explicit CLAUDE.md violation)
- "Great point!" / "Excellent feedback!" (performative)
- "Let me implement that now" (before verification)

**INSTEAD:**
- Restate the technical requirement
- Ask clarifying questions
- Push back with technical reasoning if wrong
- Just start working (actions > words)

## Handling Unclear Feedback

```
IF any item is unclear:
  STOP - do not implement anything yet
  ASK for clarification on unclear items

WHY: Items may be related. Partial understanding = wrong implementation.
```

**Example:**
```
your human partner: "Fix 1-6"
You understand 1,2,3,6. Unclear on 4,5.

❌ WRONG: Implement 1,2,3,6 now, ask about 4,5 later
✅ RIGHT: "I understand items 1,2,3,6. Need clarificat
…(생략)

## Codex 리뷰 기록

### Round 1

```
OpenAI Codex v0.146.0
--------
workdir: /Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream
model: gpt-5.4
provider: openai
approval: never
sandbox: read-only
reasoning effort: medium
reasoning summaries: none
session id: 019fd107-15d4-7830-85ea-6c08c0176ed9
--------
user
current changes
exec
/bin/zsh -lc "git status --short && echo '---DIFF---' && git diff --staged --stat && git diff --stat && echo '---FILES---' && find .. -name AGENTS.md -o -name AGENTS.override.md" in /Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream
 succeeded in 4475ms:
../03_gugbab-claude-dream/AGENTS.md

exec
/bin/zsh -lc 'cat AGENTS.md' in /Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream
 succeeded in 0ms:
<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

exec
/bin/zsh -lc 'git diff -- lib/chat-history.ts lib/chat-history.test.ts lib/chat-stream.ts' in /Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream
 succeeded in 0ms:
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-Macfcn0p' (errno=Operation not permitted)
git: warning: confstr() failed with code 5: couldn't get path of DARWIN_USER_TEMP_DIR; using /tmp instead
git: error: couldn't create cache file '/tmp/xcrun_db-N9h85uhj' (errno=Operation not permitted)
diff --git a/lib/chat-history.test.ts b/lib/chat-history.test.ts
index b4a2fbd..6035d78 100644
--- a/lib/chat-history.test.ts
+++ b/lib/chat-history.test.ts
@@ -41,9 +41,20 @@ describe("toOutgoingMessages", () => {
     });
 
     it("잘라낸 뒤 첫 메시지가 model이면 제거한다 (relay 규약: 첫 메시지는 user)", () => {
-        const messages = turns(80);
+        // 81개: slice(-30)이 인덱스 51(model)에서 시작 → 드롭 분기가 실제로 실행됨
+        const messages = turns(81);
         const out = toOutgoingMessages(messages);
+        expect(out.length).toBe(29); // 30개 중 선두 model 1개 제거
         expect(out[0].role).toBe("user");
+        expect(out[0].content).toBe("메시지 52");
+    });
+
+    it("윈도우에 user 메시지가 하나도 없으면 빈 배열을 반환한다 (relay 규약 위반 방지)", () => {
+        const allModel: Turn[] = Array.from({ length: 3 }, (_, i) => ({
+            role: "model" as const,
+            content: `답변 ${i}`,
+        }));
+        expect(toOutgoingMessages(allModel)).toEqual([]);
     });
 
     it("빈 이력은 빈 배열을 반환한다", () => {
diff --git a/lib/chat-history.ts b/lib/chat-history.ts
index 37c8504..db0ecf3 100644
--- a/lib/chat-history.ts
+++ b/lib/chat-history.ts
@@ -42,7 +42,9 @@ function clampContent(content: string): string {
 export function toOutgoingMessages(messages: readonly HistoryMessage[]): OutgoingMessage[] {
     const sliced = messages.slice(-MAX_OUTGOING_COUNT);
     const firstUserIdx = sliced.findIndex((m) => m.role === "user");
-    const window = firstUserIdx > 0 ? sliced.slice(firstUserIdx) : sliced;
+    // user 메시지가 하나도 없으면 빈 배열 — relay 규약 위반 payload를 만들지 않는다
+    if (firstUserIdx === -1) return [];
+    const window = sliced.slice(firstUserIdx);
     return window.map((m, i) => {
         const isRecent = i >= window.length - RECENT_VERBATIM_COUNT;
         if (!isRecent && m.role === "model" && m.summary) {
diff --git a/lib/chat-stream.ts b/lib/chat-stream.ts
index 0e1b4c1..16b329e 100644
--- a/lib/chat-stream.ts
+++ b/lib/chat-stream.ts
@@ -60,7 +60,8 @@ export async function streamChat(
         } else if (event.type === "done") {
             // relay가 modelId를 생략해도 문자열 계약 유지
             modelId = event.modelId ?? "";
-            summary = event.summary;
+            // JSON.parse 캐스트 경계 — 비문자열 summary가 IDB로 흘러들지 않게 가드
+            summary = typeof event.summary === "string" ? event.summary : undefined;
         } else if (event.type === "error") {
             throw new Error(event.message);
         }

exec
/bin/zsh -lc "sed -n '1,220p' lib/chat-history.ts" in /Users/lf/Desktop/gugbab-workspace/03_gugbab-claude-dream
 succeeded in 0ms:
/**
 * 채팅 이력 전송 한도 — /api/chat zod 스키마와 클라이언트 전송 압축이 반드시 이 상수를 공유한다.
 * (긴 해몽 답변이 이력에 쌓이면 API가 400으로 거부해 방 전체가 막히는 문제 방지)
 */
export const MESSAGE_LIMITS = {
    maxContentLength: 4000,
    maxCount: 50,
} as const;

/** /api/chat로 전송하는 메시지 형태 */
export interface OutgoingMessage {
    role: "user" | "model";
    content: string;
}

/** 압축 입력 형태 — model 메시지는 relay가 생성한 요약(best-effort)을 가질 수 있다 */
export interface HistoryMessage extends OutgoingMessage {
    summary?: string;
}

// 전송 이력 상한 — API 상한(50)보다 낮게 잡아 여유를 둔다
const MAX_OUTGOING_COUNT = 30;
// 최근 N개는 원문 유지 — 직전 턴의 뉘앙스가 답변 품질에 직결되므로 요약 대체 대상에서 제외
const RECENT_VERBATIM_COUNT = 4;
const TRUNCATION_MARK = "\n…(이하 생략)";
const SUMMARY_PREFIX = "[이전 답변 요약] ";

…(733줄 생략)
```

### Round 2

```
[codex] Starting Codex task thread.
[codex] Thread ready (019fcfbd-1e50-7e51-918c-97217a542768).
[codex] Turn started (019fcfbd-1f28-7c90-8609-c4d0b04e8091).
[codex] Running command: /bin/zsh -lc 'git diff -- lib/sandbox.ts lib/__tests__/sandbox.test.ts'
[codex] Command completed: /bin/zsh -lc 'git diff -- lib/sandbox.ts lib/__tests__/sandbox.test.ts' (exit 0)
[codex] Running command: /bin/zsh -lc 'git diff --stat -- lib/sandbox.ts lib/__tests__/sandbox.test.ts'
[codex] Command completed: /bin/zsh -lc 'git diff --stat -- lib/sandbox.ts lib/__tests__/sandbox.test.ts' (exit 0)
[codex] Running command: /bin/zsh -lc 'git status --short'
[codex] Command completed: /bin/zsh -lc 'git status --short' (exit 0)
[codex] Running command: /bin/zsh -lc "nl -ba lib/sandbox.ts | sed -n '1,240p'"
[codex] Command completed: /bin/zsh -lc "nl -ba lib/sandbox.ts | sed -n '1,240p'" (exit 0)
[codex] Running command: /bin/zsh -lc "nl -ba lib/__tests__/sandbox.test.ts | sed -n '320,520p'"
[codex] Command completed: /bin/zsh -lc "nl -ba lib/__tests__/sandbox.test.ts | sed -n '320,520p'" (exit 0)
[codex] Running command: /bin/zsh -lc "rg -n \"runLocalStream|makeLineParser|type: \\\"done\\\"|type: \\\"error\\\"\" ...
[codex] Command completed: /bin/zsh -lc "rg -n \"runLocalStream|makeLineParser|type: \\\"done\\\"|type: \\\"error\\\"\" ... (exit 0)
[codex] Running command: /bin/zsh -lc "nl -ba lib/__tests__/sandbox.test.ts | sed -n '520,680p'"
[codex] Command completed: /bin/zsh -lc "nl -ba lib/__tests__/sandbox.test.ts | sed -n '520,680p'" (exit 0)
[codex] Running command: /bin/zsh -lc "nl -ba lib/sandbox.ts | sed -n '240,390p'"
[codex] Command completed: /bin/zsh -lc "nl -ba lib/sandbox.ts | sed -n '240,390p'" (exit 0)
[codex] Assistant message captured: {"verdict":"needs-attention","summary":"No-ship: the new delta-first parser silently truncate...
[codex] Turn completed.
# Codex Adversarial Review

Target: working tree diff
Verdict: needs-attention

No-ship: the new delta-first parser silently truncates responses when any streamed delta is lost or altered, and it still reports a normal `done` event.

Findings:
- [high] Prefix-mismatch fallback drops authoritative final snapshot and can return truncated output as success (lib/sandbox.ts:140-164)
  `assistant` snapshots are now accepted only when `block.text` is a strict prefix-extension of the accumulated `text_delta` stream. If any delta is skipped, duplicated, or otherwise diverges, the final full snapshot is ignored and the relay completes from the stale partial buffer. The new test at the same location explicitly locks in this behavior for a mismatch case. That means a transient stream defect turns into silent user-visible data loss: the client gets an incomplete answer and possibly no summary, but still receives a normal `done`. This is especially risky because the authoritative full snapshot is available precisely at block completion and could be used to detect or repair the divergence.
  Recommendation: Do not treat a non-prefix final snapshot as ignorable success. On mismatch, either resynchronize from the authoritative snapshot in a way that preserves already-emitted text, or surface an explicit error/incomplete status instead of emitting `done` from the partial buffer.

Next steps:
- Add a mismatch-handling path for `assistant` snapshots and verify that divergent delta streams cannot end in silent truncation.
- Add a test where `text_delta` content is missing or altered but the final `assistant` snapshot contains the complete answer, and require either successful resync or an explicit failure signal.

```

### Round 3

```
[codex] Starting Codex task thread.
[codex] Thread ready (019fcfbf-6da5-7c60-af1e-c5afc3d949ab).
[codex] Turn started (019fcfbf-6e6b-7b22-8770-f5ce779908e0).
[codex] Running command: /bin/zsh -lc 'git diff -- lib/sandbox.ts lib/__tests__/sandbox.test.ts memory/project_relay_p...
[codex] Command completed: /bin/zsh -lc 'git diff -- lib/sandbox.ts lib/__tests__/sandbox.test.ts memory/project_relay_p... (exit 0)
[codex] Running command: /bin/zsh -lc 'git status --short'
[codex] Command completed: /bin/zsh -lc 'git status --short' (exit 0)
[codex] Running command: /bin/zsh -lc "nl -ba lib/sandbox.ts | sed -n '1,260p'"
[codex] Running command: /bin/zsh -lc "nl -ba lib/__tests__/sandbox.test.ts | sed -n '1,320p'"
[codex] Command completed: /bin/zsh -lc "nl -ba lib/sandbox.ts | sed -n '1,260p'" (exit 0)
[codex] Command completed: /bin/zsh -lc "nl -ba lib/__tests__/sandbox.test.ts | sed -n '1,320p'" (exit 0)
[codex] Running command: /bin/zsh -lc "nl -ba lib/sandbox.ts | sed -n '260,420p'"
[codex] Command completed: /bin/zsh -lc "nl -ba lib/sandbox.ts | sed -n '260,420p'" (exit 0)
[codex] Running command: /bin/zsh -lc "rg -n \"runClaudeStream|logs\\(|makeLineParser|stream_event|result\" lib/sandbo...
[codex] Command completed: /bin/zsh -lc "rg -n \"runClaudeStream|logs\\(|makeLineParser|stream_event|result\" lib/sandbo... (exit 0)
[codex] Running command: /bin/zsh -lc "nl -ba lib/__tests__/sandbox.test.ts | sed -n '320,520p'"
[codex] Command completed: /bin/zsh -lc "nl -ba lib/__tests__/sandbox.test.ts | sed -n '320,520p'" (exit 0)
[codex] Running command: /bin/zsh -lc "nl -ba lib/__tests__/sandbox.test.ts | sed -n '520,700p'"
[codex] Command completed: /bin/zsh -lc "nl -ba lib/__tests__/sandbox.test.ts | sed -n '520,700p'" (exit 0)
[codex] Assistant message captured: {"verdict":"needs-attention","summary":"No-ship: the new stream parser can falsely fail valid...
[codex] Turn completed.
# Codex Adversarial Review

Target: working tree diff
Verdict: needs-attention

No-ship: the new stream parser can falsely fail valid assistant payloads and still leak corrupted output on abnormal termination.

Findings:
- [high] Valid multi-block assistant messages are treated as stream corruption (lib/sandbox.ts:146-154)
  `assistant` events carry `message.content` as an array, but this code validates each `text` block independently against the already-emitted prefix and marks the whole stream inconsistent if any later block does not start with that prefix. For a legitimate assistant message with multiple text blocks, the second block will usually fail this check even though the overall message is valid. After that, the request ends in an `error` instead of `done`. This is a user-visible regression introduced by the new inconsistency guard.
  Recommendation: Reconstruct the authoritative assistant snapshot from all text blocks in order, then compare that combined string once against the emitted prefix. Add a test covering an `assistant` event with multiple `text` blocks.
- [high] Detected corruption does not stop further streaming, and abnormal close can still flush bad text without an error (lib/sandbox.ts:130-190)
  After `inconsistent` is set, the parser keeps accepting later `text_delta` events and may continue streaming chunks from the stale `fullText`. Worse, `finalize()` ignores `inconsistent` entirely and flushes the buffered tail on abnormal termination. If the upstream process crashes after a mismatch but before `result`, clients can receive additional corrupted text with no terminal error, which is hard to detect or roll back once chunks are rendered.
  Recommendation: Fail closed as soon as a snapshot contradicts emitted output: stop emitting further chunks, optionally abort the upstream command, and make `finalize()` emit an error (or at least suppress buffered flush) when `inconsistent` is true. Add a crash-path test for mismatch followed by stream close without `result`.

Next steps:
- Fix assistant snapshot reconciliation before shipping.
- Add regression tests for multi-block assistant payloads and mismatch-plus-abnormal-close behavior.

```
